# synced at build time from research-docs/ — never edit here, never commit
/content/

node_modules/
.next/
out/
dist/
/public/research-assets/
# registry.json copied to the site root by sync-content (ADR-0039) — never commit
/public/registry.json

# local model artifacts for /interfaces (weights never enter the tree)
/public/artifacts/

# pre-release dev exports served during local development (same rule)
/public/dev-models/

# LLM-readable markdown generated by scripts/build-text.mjs (never edit/commit)
/public/research/
/public/models/
/public/train.md
/public/api.md
/public/llms.txt
/public/llms-full.txt
/public/sitemap.xml
