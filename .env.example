# Copy to .env.local or .env (both gitignored). tools/synthgen reads
# OPENROUTER_API_KEY from the process environment first, then .env.local,
# then .env — first non-empty value wins (ADR-0038).
OPENROUTER_API_KEY=
